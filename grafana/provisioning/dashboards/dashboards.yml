apiVersion: 1

providers:
  - name: 'Observatory Dashboards'
    orgId: 1
    folder: 'Observatory'
    folderUid: 'observatory'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
